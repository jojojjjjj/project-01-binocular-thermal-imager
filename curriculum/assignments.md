# 作业说明 | Assignments Overview

> 本文档定义了双目热成像仪（High 难度）项目的全部作业。所有作业遵循 [ASSIGNMENT_DESIGN_STANDARD.md](../../example_course_course_work/ASSIGNMENT_DESIGN_STANDARD.md) 标准。

---

## 作业总览 | Assignment Summary

本项目共设 **4 份核心正式作业（HW1/HW2/HW3/HW4）+ 2 份 Check-in Report + HW6 最终展示与反思（含原 Phase Review 的设计权衡附录）+ 15 天每日课后任务**。原 HW5 已并入 HW4，原 Phase Review 已并入 HW6。

| 编号 | 作业 | 截止日期 | 权重 | 核心产出 |
|------|------|---------|------|---------|
| HW1 | 嵌入式开发环境与硬件基础 | Day 3 结束时 | 15% | 可编译的 STM32 工程 + I2C 扫描成功 |
| Check-in 1 | Phase 1-2 进度汇报 | Day 7 结束时 | 8% | 进度汇报文档 |
| HW2 | MLX90640 传感器驱动与温度解析 | Day 7 结束时 | 18% | 完整的温度读取与解析固件 |
| HW3 | USB CDC 通信与数据协议设计 | Day 10 结束时 | 18% | 稳定 USB 传输 + Python 验证脚本 |
| Check-in 2 | Phase 3-4 进度汇报 | Day 13 结束时 | 8% | 进度汇报文档 |
| HW4 | 热图显示与融合体验（PC 端可视化） | Day 13 结束时 | 28% | PC 端热图 + 伪彩 + 融合脚本 |
| HW5 | （已并入 HW4） | — | 0% | 见 HW4 |
| Phase Review | （已并入 HW6 反思） | — | 0% | 见 HW6 |
| HW6 | 最终展示与项目文档 | Day 15 结束时 | 0%* | PPT + Demo + README + 演示 |

> *HW6 不单独计权重，其分数计入"演示展示"维度（总评分的 25%，含原 Phase Review 5%），作为最终评审依据。

### 作业 Pipeline 依赖链

```
HW1（环境+GPIO+I2C 基础）
  ├─ 产出：STM32 工程可编译、I2C 总线扫描到 MLX90640
  └─→ HW2 依赖此基础，在其上开发传感器驱动

HW2（传感器驱动+温度解析）
  ├─ 产出：能读取并解析 32×24 温度矩阵
  └─→ HW3 依赖此基础，将温度数据通过 USB 传输出去

HW3（USB 通信+数据协议）
  ├─ 产出：USB CDC 稳定传输温度帧 + Python 端解析验证
  └─→ HW4 依赖此基础，PC 脚本从 USB 接收温度数据

HW4（热图显示与融合体验，PC 端）
  ├─ 产出：PC 脚本能接收温度、渲染伪彩热图、并叠加可见光做融合
  └─→ HW6 依赖此基础，准备最终展示材料（原 HW5 已并入 HW4）





HW6（最终展示+文档+反思，含原 Phase Review 设计权衡附录）
  └─ 产出：PPT + 实物演示 + 完整项目文档
```

### 知识深度分布

| 层级 | 占比 | 说明 |
|------|------|------|
| **Layer 1: Know-What** | 30% | 识别、描述、画图——知道每个模块是什么、协议怎么工作 |
| **Layer 2: Know-Why** | 35% | 设计权衡、原理追问——理解为什么这样选择而不是那样 |
| **Layer 3: Know-How** | 35% | 实际实现——能完成核心代码编写、调试和集成 |

---

## 每日课后任务 | Daily After-Class Tasks

> 每天课后花 30-45 分钟完成。不单独评分，但计入 Check-in Report 和"自主学习"维度评估。

| 天数 | 任务 | 产出 | 预计时间 |
|------|------|------|---------|
| Day 1 | 画出热成像仪的系统框图（传感器→MCU→USB→PC 可视化），用不同颜色标注数据流方向；在 notes.md 中写下你对项目的 3 个疑问 | 框图（手绘拍照即可）+ 3 个疑问 | 30 min |
| Day 2 | 用自己的话解释 while(1) 主循环的作用（类比生活中的例子）；修改 LED 闪烁频率，记录不同频率下观察到的效果 | 类比文字 + 代码修改截图 | 30 min |
| Day 3 | 画出 I2C 写操作的时序图（起始位→设备地址→ACK→数据→停止位）；解释为什么 I2C 需要上拉电阻 | 时序图 + 解释文字 | 35 min |
| Day 4 | 从 MLX90640 数据手册中摘录 5 个关键参数（分辨率、测温范围、刷新率、工作电压、接口类型），用自己的话解释每个参数对项目的影响 | 参数表 + 影响分析 | 35 min |
| Day 5 | 记录你读到的 EEPROM 校准参数（前 5 个值）；思考：为什么每个传感器的校准参数不同？如果用了别人的校准数据会怎样？ | 参数记录 + 思考文字 | 30 min |
| Day 6 | 画出从原始 ADC 数据到温度值的计算流程图；用手掌测试传感器，记录观察到的温度范围 | 流程图 + 测试记录 | 35 min |
| Day 7 | 完成 **Check-in 1**（见下方）；总结 Phase 2 中最大的 3 个技术困难及解决方法 | Check-in 文档 | 45 min |
| Day 8 | 画出 USB CDC 数据流图（MCU 端发送→USB 线缆→PC 端口→串口工具）；解释为什么 USB CDC 比传统串口更适合本项目 | 数据流图 + 对比文字 | 30 min |
| Day 9 | 编写或完善你的数据帧协议文档（帧头、温度数据格式、帧尾）；用 Python 解析一帧数据并验证校验 | 协议文档 + Python 脚本 | 40 min |
| Day 10 | 列出固件集成后遇到的 3 个 Bug，按"现象→原因分析→解决方法"格式记录；用温度计对比传感器精度 | Bug 记录 + 精度对比 | 35 min |
| Day 11 | 用自己的话解释串口、帧解析、伪彩色映射三个 PC 端概念的作用（各 2-3 句话）；截图 PC 可视化脚本运行效果（热图窗口） | 概念解释 + 截图 | 35 min |
| Day 12 | 画出伪彩色映射的流程图（温度值→归一化→查表→RGB）；比较 Ironbow 和 Rainbow 两种配色方案的适用场景 | 流程图 + 方案对比 | 30 min |
| Day 13 | 完成 **Check-in 2**（见下方）；拍一张 PC 脚本显示热图的屏幕照片 | Check-in 文档 + 截图 | 45 min |
| Day 14 | 完成系统联调记录表（测试项、预期结果、实际结果、是否通过）；准备至少 3 分钟的演示预演 | 联调记录 + 预演笔记 | 40 min |
| Day 15 | 写一段 300 字的项目感言（学到了什么、最困难的时刻、最大的收获） | 感言文字 | 15 min |

---

# HW1: 嵌入式开发环境与硬件基础 | Embedded Dev Environment & Hardware Fundamentals

> **截止日期**：Day 3 结束时 | **权重**：15% | **预计耗时**：课后 3-4 小时

## 学习成果 | Learning Outcomes

完成本作业后，你将能够：

1. **识别** STM32 微控制器的核心架构组件（内核、Flash、SRAM、总线、外设）及其功能
2. **解释** I2C 通信协议的工作原理以及本项目选择 I2C 连接热传感器的理由
3. **实现** 一个能在 STM32 上运行的 GPIO 控制程序（LED 闪烁）和 I2C 总线扫描程序
4. **分析** 硬件开发中的常见问题（烧录失败、I2C 无响应）并制定排查策略

## 为什么学这个 | Why This Matters

开发环境和硬件基础是整个项目的**第一步**。如果你没法编译代码、没法烧录到芯片、没法让 I2C 总线工作，后面所有传感器驱动和 APP 开发都是空中楼阁。这就像学做饭前先要认识厨房——知道灶台在哪、调料放在哪、怎么开火，才能开始做菜。

**日常类比**：GPIO 控制就像学开车时先学会打方向盘——最基础的操作，但一切从这里开始。I2C 总线扫描就像在一个房间里找人——你喊一声"谁在？"，每个人报自己的名字（设备地址），这样你才知道房间里有哪些人可以合作。

## 前置知识 | Prerequisites

- 已阅读 prerequisites.md 中的基础知识
- 已安装 STM32CubeIDE（Day 1 完成）
- 了解 C 语言基本语法（变量、函数、循环）
- 了解简单电路概念（电压、电流、电阻）

## 任务清单 | Task List

### 任务 1: STM32 工程创建与编译验证 (难度: ⭐)

**步骤**：

1. 打开 STM32CubeIDE，创建新工程：File → New → STM32 Project
2. 选择芯片型号：STM32F411CEU6
3. 工程命名为 ThermalEyes_Hw1，语言选择 C
4. 在 CubeMX 中配置 PC13 为 GPIO_Output，User Label 设为 LED
5. 点击 Project → Build All（或按 Ctrl+B），确认编译成功
6. 将工程导出为 ZIP 文件备用

**预期产出**：
- 编译成功的 Console 输出截图（显示 0 errors, 0 warnings）
- 工程目录结构截图

**验证方法**：Console 输出显示 0 errors 即为成功。

### 任务 2: LED 闪烁与 GPIO 控制 (难度: ⭐)

**步骤**：

1. 在 main.c 的 USER CODE BEGIN 3 区域添加 LED 闪烁代码：
   `c
   HAL_GPIO_TogglePin(LED_GPIO_Port, LED_Pin);
   HAL_Delay(500);
   `
2. 编译并烧录到开发板，确认 LED 以 500ms 间隔闪烁
3. 修改延迟为 100ms 和 1000ms，分别观察效果
4. 添加按键控制：配置 PA0 为 GPIO_Input，上拉模式，实现按下按键切换 LED 状态

**预期产出**：
- LED 闪烁效果照片
- 按键控制代码截图

**验证方法**：LED 能闪烁、按键能切换 LED 状态即为成功。

### 任务 3: I2C 配置与总线扫描 (难度: ⭐⭐)

**步骤**：

1. 在 CubeMX 中配置 I2C1：PB6 → I2C1_SCL，PB7 → I2C1_SDA，I2C 模式设为 I2C，速度保持默认 100kHz
2. 连接 MLX90640 模块到开发板：SDA→PB7，SCL→PB6，VIN→3.3V，GND→GND
3. 在 main.c 中实现 I2C 扫描程序，扫描地址 0x03 到 0x77：
   `c
   for (uint8_t addr = 3; addr < 0x78; addr++) {
       if (HAL_I2C_IsDeviceReady(&hi2c1, addr << 1, 3, 10) == HAL_OK) {
           printf("Found device at 0x%02X\r\n", addr);
       }
   }
   `
4. 配置 USB 虚拟串口或 UART 重定向 printf，查看扫描结果
5. 确认扫描到 MLX90640（地址 0x33）

**预期产出**：
- I2C 扫描结果截图（显示 Found device at 0x33）
- I2C 接线照片

**验证方法**：串口输出中出现 0x33 即为成功。如果扫描不到，检查接线和上拉电阻。

### 任务 4: 理解 I2C 协议的"为什么" (难度: ⭐⭐)

**步骤**：

1. 阅读理解 I2C 通信的关键概念：起始位、设备地址、ACK/NACK、数据字节、停止位
2. 在笔记本上画出 I2C 写操作的时序图，标注每个阶段
3. 思考并记录：为什么 I2C 总线需要上拉电阻？如果没有上拉电阻会怎样？
4. 思考并记录：为什么 I2C 地址是 7 位但传输时左移 1 位（变成 8 位）？

**预期产出**：
- 手绘 I2C 时序图（拍照提交）
- 两个"为什么"问题的书面回答

**验证方法**：时序图包含起始位、地址位、ACK、数据位、停止位即为合格。

## 理解验证问题 | Comprehension Check

**Q1（设计权衡题）**：为什么选 STM32F411 而不是 Arduino Nano？

本项目选择 STM32F411 作为主控芯片，而不是更简单的 Arduino Nano（ATmega328P）。请从以下三个角度分析这个设计选择的理由：

1. **USB 通信能力**：本项目需要通过 USB 与手机通信，两款芯片在这方面有什么差异？
2. **处理速度**：MLX90640 每帧 768 个像素点，需要在短时间内完成读取和打包，速度够不够？
3. **学习价值**：用 Arduino 只需 Wire.read()，用 STM32 要理解 HAL 库。哪种方式对"理解嵌入式系统"更有帮助？

> **参考答案**：
> 
> 1. **USB 通信能力**：STM32F411 有硬件 USB OTG 控制器，可以直接实现 USB CDC 虚拟串口，传输速率可达 12Mbps。Arduino Nano 的 ATmega328P 没有硬件 USB，只能用软件模拟 USB（如 V-USB 库），速度慢且不稳定。对于本项目需要实时传输温度帧数据，硬件 USB 是刚需。
> 
> 2. **处理速度**：STM32F411 的 Cortex-M4 运行在 100MHz，ATmega328P 只有 16MHz。读取一帧 MLX90640 数据需要多次 I2C 事务（2 个半帧 × 32×12 字节），STM32 可以轻松在 50ms 内完成，Arduino 可能需要 200ms 以上，影响帧率。此外，STM32 的 DMA 功能可以进一步减轻 CPU 负担。
> 
> 3. **学习价值**：Arduino 封装了很多细节（Wire.read() 背后是完整的状态机），使用简单但"知其然不知其所以然"。STM32 + HAL 库虽然学习曲线陡，但你能理解 I2C 外设的配置参数（时钟速度、地址模式、ACK 机制），遇到问题能从底层排查。这就好比学开车：自动挡（Arduino）容易上手，但手动挡（STM32）让你真正理解发动机和变速箱的工作方式。

**Q2（原理追问题）**：I2C 上拉电阻

I2C 总线的 SDA 和 SCL 线上都需要上拉电阻（通常 4.7kΩ）。请回答：

1. 为什么 I2C 设备的输出是"开漏"（Open-Drain）而不是"推挽"（Push-Pull）？
2. 如果上拉电阻太大（比如 100kΩ），信号会出现什么问题？
3. 如果上拉电阻太小（比如 1kΩ），又会有什么问题？
4. 为什么 SPI 不需要上拉电阻？

> **参考答案**：
> 
> 1. I2C 是多设备共享总线，多个设备可以同时拉低信号线。开漏输出意味着设备只能把线拉低（输出低电平），不能拉高。拉高由上拉电阻完成。这样当多个设备同时操作时，不会出现一个设备输出高、另一个输出低导致的短路冲突。这就像开会时大家用"举手"表示发言——没人举手时默认安静（上拉），有人举手时信号有效（下拉）。
> 
> 2. 上拉电阻太大，RC 时间常数增大，信号上升沿变慢（方波变成了圆弧波），可能导致信号在高频时不满足时序要求，通信失败。就像弹簧太松，拉回来太慢，跟不上节奏。
> 
> 3. 上拉电阻太小，所有设备拉低信号线时电流过大（I = V/R = 3.3V/1kΩ = 3.3mA 每条线），增加功耗，而且可能超出 I2C 设备的灌电流能力（通常最大 3mA）。就像弹簧太紧，虽然回弹快，但需要很大力气才能按下。
> 
> 4. SPI 是全双工通信，每条线有明确的方向（MOSI 主→从，MISO 从→主），同一时刻只有一个设备驱动每条线，因此可以用推挽输出，不需要上拉。I2C 是半双工，SDA 线双向共用，需要开漏+上拉来实现"线与"功能。

**Q3（类比迁移题）**：为什么嵌入式的程序需要 while(1) 死循环？

PC 上的程序通常执行完就退出了，但嵌入式程序总是在 while(1) 里无限循环。请用一个生活中的类比来解释为什么嵌入式程序需要这样设计。

> **参考答案**：
> 
> 类比：嵌入式程序就像红绿灯控制器。你不会希望红绿灯运行一会儿就"结束"了——它需要 24 小时不停地工作，按照固定的逻辑循环：绿灯→黄灯→红灯→绿灯……这就是 while(1) 的含义：永远重复执行主循环中的任务。
> 
> PC 程序更像"算一道数学题"——算完就结束了。但嵌入式系统更像"当保安"——不是完成一个任务就走，而是持续监听、持续响应。传感器数据需要不断读取，按键需要不断检测，LED 状态需要不断更新，这些都是在 while(1) 循环中完成的。

## 提交要求 | Submission Requirements

请将以下文件打包提交到 Git 仓库：

1. **代码**：irmware/ThermalEyes_Hw1/ — 完整 STM32 工程（含 LED 闪烁 + I2C 扫描代码）
2. **截图/照片**：
   - hw1_build_success.png — 编译成功截图
   - hw1_led_blink.jpg — LED 闪烁效果照片
   - hw1_i2c_scan.png — I2C 扫描结果截图（显示 0x33）
   - hw1_wiring.jpg — MLX90640 接线照片
3. **理解验证回答**：hw1_comprehension.md（5 个问题，每个 100-200 字）
4. **I2C 时序图**：hw1_i2c_timing.jpg（手绘拍照或电子绘图均可）

**代码规范检查清单**：
- [ ] main.c 中用户代码在 USER CODE BEGIN/END 区域内
- [ ] GPIO 和 I2C 配置通过 CubeMX 生成，不在用户代码中直接修改
- [ ] 关键代码有注释说明功能
- [ ] I2C 扫描结果通过串口输出（不是调试器查看寄存器）

## 评分标准 | Rubric

| 维度 | 满分 | 评分细则 |
|------|------|---------|
| **任务完成度** | 35 | 任务 1（8分）+ 任务 2（8分）+ 任务 3（12分）+ 任务 4（7分）。部分完成按完成程度给分 |
| **理解验证质量** | 30 | 每题 6 分。能从多角度分析且有深入见解得 5-6 分；有分析但浅显得 3-4 分；仅复述事实得 1-2 分 |
| **代码规范** | 15 | 代码在 USER CODE 区域、有注释、CubeMX 配置规范得 12-15 分；基本规范得 8-11 分；缺少注释得 0-7 分 |
| **I2C 调试记录** | 10 | 有完整的接线照片、扫描结果截图，如遇到问题有排查记录得 8-10 分；基本完整得 5-7 分；缺少截图得 0-4 分 |
| **时序图质量** | 10 | 时序图清晰、标注完整、能反映 I2C 写操作流程得 8-10 分；基本正确得 5-7 分；不完整得 0-4 分 |
| **总计** | **100** | — |

---

# HW2: MLX90640 传感器驱动与温度解析 | MLX90640 Sensor Driver & Temperature Parsing

> **截止日期**：Day 7 结束时 | **权重**：18% | **预计耗时**：课后 4-5 小时

## 学习成果 | Learning Outcomes

完成本作业后，你将能够：

1. **识别** MLX90640 红外热成像传感器的内部结构（像素阵列、EEPROM、ADC）及关键寄存器
2. **解释** 温度计算公式中每一步的物理意义，以及每个传感器需要单独校准的原因
3. **实现** 完整的 MLX90640 驱动程序（EEPROM 读取、帧数据获取、温度计算、坏像素补偿）
4. **分析** 传感器数据的合理性，识别并处理异常数据

## 为什么学这个 | Why This Matters

传感器驱动是整个项目的**数据源头**。如果温度数据不准确，后面 USB 传输再快、APP 渲染再漂亮，显示的也是错误的热图。理解传感器的工作原理和数据处理流程，就像理解相机如何把光变成照片——只有知道底层的原理，才能在出现问题时准确定位。

**日常类比**：读取传感器数据就像做化学实验——你看到的不是温度，而是一堆"仪器读数"（原始 ADC 值）。你需要用公式把这些原始读数"换算"成真正的温度，就像用滴定数据算浓度。如果换算公式错了（比如用了别人的校准参数），就像用错了换算系数，结果一定不准。

## 前置知识 | Prerequisites

- HW1 已完成：I2C 通信正常，能扫描到 MLX90640（0x33）
- 已下载 MLX90640 数据手册（PDF）
- 理解二进制补码表示法（用于负温度值）

## 任务清单 | Task List

### 任务 1: 读取传感器 ID 与配置寄存器 (难度: ⭐)

**步骤**：

1. 使用 HAL_I2C_Mem_Read 读取 MLX90640 的 Chip ID（寄存器地址 0x2407…0x240D）
2. 读取控制寄存器（0x240D 和 0x240E），解析当前刷新率和分辨率设置
3. 通过串口打印读取到的所有信息
4. 对比数据手册，确认 Chip ID 正确

**预期产出**：串口输出截图，包含 Chip ID 和控制寄存器值

**验证方法**：Chip ID 值与数据手册一致。

### 任务 2: 用现成驱动读出温度矩阵 (难度: ⭐)

**步骤**：

1. 不必从零写校准公式——直接用现成驱动读温度。本课程已提供参考实现 `software/firmware/mlx90640.c`（基于 Melexis 官方算法 + colourfate 开源实现），你也可以参考 Melexis 官方库或 colourfate 的 `mlx90640.c`
2. 调用高层 API 读一帧温度：
   `c
   MLX90640_Init(&hi2c1);
   float temp_matrix[24][32];
   MLX90640_GetFrame(temp_matrix);  // 现成驱动内部已读 EEPROM 校准参数、算补偿
   `
3. 通过串口输出温度矩阵（每行 32 个值，共 24 行）
4. **理解而不是实现**：读 `mlx90640.c` 里的 `MLX90640_GetFrame`，看懂它内部做了哪几步（读 EEPROM 校准参数 → 读半帧原始数据 → Ta/Vdd 补偿 → 像素温度计算），但不要求你从零手写这些公式（公式原理放在理解验证题里问「为什么」）

**预期产出**：32×24 温度矩阵串口输出截图 + 调用现成驱动的 main.c 代码

**验证方法**：室温环境下，温度值在 20-30°C 范围内。用手掌覆盖传感器，对应像素温度应明显上升。

### 任务 3: 坏像素补偿与数据滤波 (难度: ⭐⭐⭐)

**步骤**：

1. 实现坏像素检测：将温度矩阵中与四邻域平均值偏差超过 20°C 的像素标记为坏像素
2. 实现坏像素补偿：用四邻域（上下左右）有效像素的平均值替换坏像素值
3. 实现简单移动平均滤波（3×3 窗口），平滑温度数据
4. 对比滤波前后的温度矩阵差异

**预期产出**：坏像素位置列表 + 滤波前后温度矩阵对比截图 + 补偿和滤波函数代码

**验证方法**：坏像素被正确替换，滤波后数据更平滑但不模糊。

### 任务 4: 驱动代码模块化整理 (难度: ⭐⭐)

**步骤**：

1. 将驱动代码整理为独立模块：mlx90640.h（类型定义、函数声明、宏定义）和 mlx90640.c（函数实现）
2. 确保主程序 main.c 只需调用高层 API：
   `c
   MLX90640_Init(&hi2c1);
   MLX90640_GetFrame(temp_matrix);
   MLX90640_ApplyFilter(temp_matrix);
   `
3. 为每个公开函数添加 Doxygen 风格注释

**预期产出**：mlx90640.h 和 mlx90640.c 文件 + 模块化后的 main.c

**验证方法**：代码编译通过，功能与重构前一致。

## 理解验证问题 | Comprehension Check

**Q1（设计权衡题）**：为什么每个 MLX90640 需要单独校准？

MLX90640 出厂时，每个传感器都写入了独一无二的校准参数到 EEPROM。为什么不能像普通温度传感器（如 DS18B20）那样用同一个公式？从以下两个角度分析：

1. **制造差异**：红外传感器的像素阵列在制造时存在什么差异？
2. **校准成本**：如果不在出厂时校准，而是让用户自己校准，会有什么问题？

> **参考答案**：
> 
> 1. 红外热成像传感器的每个像素都是一个独立的微测辐射热计（Microbolometer），制造过程中微小的不均匀性导致每个像素的基准偏移和灵敏度都不同。这就像一个合唱团——每个人的音色和音量天生不同，如果用统一的"标准声音"来调音，听起来就会参差不齐。DS18B20 只有一个感温元件，一致性容易控制；MLX90640 有 768 个，必须逐个校准。
> 
> 2. 用户自行校准需要黑体炉（精确控温的参考源）和专业的校准流程，成本高且对普通用户不现实。出厂校准是在精确控制的环境下完成的，保证了一致性和可追溯性。就像买钢琴时已经调好了音——如果让用户自己调，大部分人是做不到的。

**Q2（原理追问题）**：为什么温度计算公式这么复杂？（现成驱动替你算了，但你要懂为什么）

MLX90640 的温度计算公式涉及 Vdd 补偿、Ta 补偿、发射率修正等多个步骤（数据手册 Section 11），而普通数字温度传感器（如 DS18B20）直接读取就是温度值。为什么热成像传感器的计算如此复杂？我们直接调现成驱动读温度，为什么不需要手写这些公式、但仍然需要懂它们的原理？

> **参考答案**：
> 
> 普通温度传感器测量的是自身温度（接触式），环境相对简单。红外热成像传感器是非接触式的——它测量的是目标物体发出的红外辐射强度，但这个强度受到多个因素影响：
> 
> 1. **传感器自身温度（Ta）**：传感器的基准电压随自身温度漂移，必须补偿。就像用弹簧秤称重——弹簧本身也会热胀冷缩，需要修正。
> 2. **供电电压（Vdd）**：ADC 的参考电压随供电波动，需要用 Vdd 补偿系数修正。就像尺子的刻度会随温度变化，需要校准。
> 3. **发射率**：不同材质的表面红外发射率不同（黑体 1.0，抛光金属可能 0.1），默认假设 1.0 可能不准确。
> 4. **像素间差异**：768 个像素各自有不同的偏移和灵敏度，需要逐像素校准。
> 
> 这些因素叠加，公式必须层层修正才能准。我们调现成驱动，是因为这些公式 Melexis 已经写好并验证过了，你重新手写既容易出错也没必要。但懂原理的意义在于：当温度明显不对（比如整体偏移 10°C）时，你能想到是"发射率设错了"还是"传感器自身温度漂了"，而不是无从下手。这就是"用现成轮子，但要懂轮子怎么转"。

## 提交要求 | Submission Requirements

1. **代码**：irmware/ThermalEyes_Hw2/ — 完整工程（调用现成 MLX90640 驱动读温度矩阵）
2. **截图/照片**：
   - hw2_chip_id.png — Chip ID 读取结果截图
   - hw2_temp_matrix.png — 32×24 温度矩阵串口输出截图
   - hw2_hand_test.jpg — 手掌覆盖传感器的温度变化照片
3. **理解验证回答**：hw2_comprehension.md（2 个问题，每个 150-250 字）

**代码规范检查清单**：
- [ ] 驱动代码已模块化（mlx90640.c/h，可直接用参考实现）
- [ ] 调用 MLX90640_Init / MLX90640_GetFrame 等高层 API，不自己写补偿公式
- [ ] 能说清 GetFrame 内部做了哪几步（读 EEPROM→读半帧→补偿→算温度）
- [ ] 温度值合理（室温 20-30°C，手掌测试有上升）

## 评分标准 | Rubric

| 维度 | 满分 | 评分细则 |
|------|------|---------|
| **任务完成度** | 35 | 任务 1（8分）+ 任务 2（15分）+ 任务 3（8分）+ 任务 4（4分）。改用现成驱动，任务2从读EEPROM升级为调API读温度矩阵 |
| **理解验证质量** | 30 | 2 题每题 15 分。能从多角度深入分析（为什么校准 / 为什么公式复杂）得 24-30 分；有分析但浅显得 15-23 分；仅复述得 0-14 分 |
| **代码质量** | 20 | 模块化清晰、注释完整、温度值合理得 16-20 分；基本可用但缺注释得 10-15 分；代码混乱得 0-9 分 |
| **数据验证** | 15 | 温度值合理（室温 ±5°C）、手掌测试有变化、坏像素已处理得 12-15 分；数据基本合理得 7-11 分；异常得 0-6 分 |
| **总计** | **100** | — |

---

# HW3: USB CDC 通信与数据协议设计 | USB CDC Communication & Data Protocol Design

> **截止日期**：Day 10 结束时 | **权重**：18% | **预计耗时**：课后 3-4 小时

## 学习成果 | Learning Outcomes

完成本作业后，你将能够：

1. **识别** USB CDC 类的功能和作用，以及 USB 协议的基本概念（设备、端点、描述符）
2. **解释** 为什么本项目选择 USB CDC 而不是传统 UART 或蓝牙来传输数据
3. **实现** 完整的 USB CDC 数据传输：固件端打包发送 + Python 端接收解析
4. **设计** 一个可靠的温度数据帧协议，并验证其正确性

## 为什么学这个 | Why This Matters

USB 通信是连接嵌入式固件和手机 APP 的**桥梁**。传感器读到的温度数据必须通过某种方式传到 APP 才能显示。理解数据从 MCU 到 APP 的完整路径，就像理解快递从仓库到家的过程——如果包装格式不对（协议设计差）、运输过程丢件（数据丢失）、收件人看不懂标签（解析错误），整个链路就断了。

**日常类比**：数据协议就像快递的包装规范——每个包裹有面单（帧头）、内容物（温度数据）、签收确认（帧尾/校验）。如果面单格式不统一，快递员不知道往哪送；如果内容物没有保护，运输中可能损坏；如果没有签收确认，你不知道对方是否收到了。

## 前置知识 | Prerequisites

- HW2 已完成：能读取并计算 32×24 温度矩阵
- 了解串口通信的基本概念
- 安装了 Python 3 和 pyserial 库

## 任务清单 | Task List

### 任务 1: USB CDC 配置与基本通信 (难度: ⭐⭐)

**步骤**：

1. 在 CubeMX 中配置 USB OTG FS 为 Device_Only 模式
2. 添加 USB CDC 类（Communication Device Class）
3. 编译烧录后，在电脑设备管理器中确认出现新的 COM 端口
4. 使用串口工具发送和接收数据，验证基本通信

**预期产出**：设备管理器截图（显示新 COM 端口）+ 串口通信测试截图

**验证方法**：串口工具能收到 STM32 发送的数据。

### 任务 2: 温度数据帧协议设计 (难度: ⭐⭐⭐)

**步骤**：

1. 设计温度数据帧格式：
   - 帧头（2字节）: 0xFF 0xFE
   - 帧序号（2字节）: 递增计数器
   - 温度数据（1536字节）: 768个像素 × 2字节（int16_t，0.01°C 单位）
   - 校验和（2字节）: 所有数据字节的异或值
   - 帧尾（2字节）: 0xFE 0xFF
2. 在固件中实现打包函数
3. 在主循环中每次读取温度后调用打包函数并通过 USB CDC 发送

**预期产出**：帧协议设计文档 + 打包和发送函数代码

**验证方法**：用串口工具观察发送的原始数据，帧头帧尾正确。

### 任务 3: Python 端数据接收与解析 (难度: ⭐⭐⭐)

**步骤**：

1. 编写 Python 脚本接收 USB CDC 数据并解析帧头、温度矩阵、校验和
2. 添加校验和验证逻辑，丢弃校验失败的帧
3. 记录连续接收 100 帧的丢帧率

**预期产出**：Python 解析脚本 + 运行结果截图 + 丢帧率统计

**验证方法**：Python 端显示的温度值与串口直接输出的一致。

### 任务 4: 固件集成与稳定性测试 (难度: ⭐⭐)

**步骤**：

1. 将 I2C 读取、温度计算、USB 发送三个模块集成到主循环
2. 运行固件 5 分钟，观察是否有崩溃或数据异常
3. 测试拔插 USB 后是否自动恢复通信

**预期产出**：集成后的 main.c 代码 + 5 分钟稳定性测试记录

**验证方法**：5 分钟内无崩溃、数据持续正常、拔插后可恢复。

### 任务 5: 面向 PC 可视化的接口文档 (难度: ⭐)

**步骤**：

1. 编写接口文档 docs/protocol.md，包含：数据帧格式说明、每个字段的字节序和数据类型、错误处理策略
2. 这份文档将作为 HW4（PC 端可视化）的输入

**预期产出**：docs/protocol.md 接口文档

**验证方法**：另一个开发者可以仅凭此文档实现 PC 端数据解析。

## 理解验证问题 | Comprehension Check

**Q1（设计权衡题）**：为什么选 USB CDC 而不是蓝牙或 WiFi？

请从延迟、带宽、复杂度三个角度分析为什么不用蓝牙（BLE）或 WiFi：

> **参考答案**：
> 
> 1. **延迟**：USB CDC 延迟约 1-5ms；BLE 延迟约 20-100ms；WiFi 延迟约 5-20ms。实时热图需要 10+ FPS，USB 和 WiFi 可以满足，BLE 会卡顿。
> 2. **带宽**：1.5KB × 10 FPS = 15KB/s。USB CDC 有效速率约 500KB/s 远超需求；BLE 约 20-50KB/s 勉强够用；WiFi 数 MB/s 绰绰有余。
> 3. **复杂度**：STM32F411 有硬件 USB，CDC 是标准类，HAL 库直接支持。BLE 需要额外模块和协议栈；WiFi 需要额外模块和 TCP/IP 协议栈。
> 
> 综合来看，USB CDC 在三个维度都是最优选择，唯一缺点是需要物理连接。

**Q2（故障分析题）**：帧协议设计中帧头和校验的作用

1. 如果没有帧头，接收端如何知道一帧从哪里开始？
2. 如果温度数据中恰好出现了 0xFF 0xFE，会不会误判为帧头？
3. 如果没有校验和，数据在传输中出错会怎样？

> **参考答案**：
> 
> 1. 没有帧头，接收端不知道数据起始位置。如果中途开始监听，整个解析会错位。帧头就像快递包裹上的"起点标记"。
> 2. 不会。因为接收端是按字节位置解析的——帧头只出现在固定位置（每 1542 字节一次），数据区域的 0xFF 0xFE 不会被误判。如果帧长度不固定，就需要"转义"机制。
> 3. 没有校验和，数据出错后无法检测。温度数据某个字节翻转可能导致某像素温度偏差很大，在热图上表现为异常亮点或暗点。校验和可以检测大多数单字节错误，让接收端丢弃错误帧。

**Q3（故障分析题）**：数据传输中丢帧的原因分析

Python 脚本接收数据，偶尔丢帧（100 帧中丢失 3-5 帧）。列出可能的原因和验证方法。

> **参考答案**：
> 
> 1. **USB 缓冲区溢出**：MCU 发送速度大于 Python 读取速度。验证：降低发送频率，观察丢帧率是否下降。
> 2. **Python 串口读取不完整**：ser.read(1542) 可能一次只读到部分数据。验证：打印每次读取的实际字节数。解决：循环读取直到凑够一帧。
> 3. **帧同步丢失**：一次读取跨越了两帧的边界。验证：实现帧头搜索逻辑，从数据流中找到 0xFF 0xFE 再解析。
> 4. **I2C 读取超时**：偶尔 I2C 通信失败导致温度数据无效。验证：固件端添加 I2C 返回值检查。

## 提交要求 | Submission Requirements

1. **代码**：irmware/ThermalEyes_Hw3/ — 完整集成工程 + scripts/usb_receiver.py — Python 脚本
2. **文档**：docs/protocol.md — 数据帧协议接口文档
3. **截图/记录**：hw3_usb_device.png + hw3_python_output.png + hw3_stability_test.md
4. **理解验证回答**：hw3_comprehension.md（3 个问题，每个 150-250 字）

**代码规范检查清单**：
- [ ] 帧打包函数有完整的参数和返回值注释
- [ ] Python 脚本包含校验和验证
- [ ] 接口文档包含所有字段的详细说明
- [ ] USB CDC 发送使用非阻塞方式或检查就绪状态
- [ ] 主循环中有 I2C 和 USB 的错误处理

## 评分标准 | Rubric

| 维度 | 满分 | 评分细则 |
|------|------|---------|
| **任务完成度** | 35 | 任务 1（8分）+ 任务 2（10分）+ 任务 3（10分）+ 任务 4（5分）+ 任务 5（2分） |
| **理解验证质量** | 30 | 3 题每题 10 分。多角度深入分析（USB选型 / 帧头校验 / 丢帧）得 24-30 分；有分析但浅显得 15-23 分；仅复述得 0-14 分 |
| **协议设计质量** | 15 | 帧格式清晰、字段完整、文档详细得 12-15 分；基本可用但文档不完整得 7-11 分；格式混乱得 0-6 分 |
| **通信稳定性** | 10 | 5 分钟稳定传输、丢帧率 < 5% 得 8-10 分；基本可用但偶尔断连得 5-7 分；频繁断连得 0-4 分 |
| **Python 脚本质量** | 10 | 解析正确、有校验和验证、有错误处理得 8-10 分；基本可用得 5-7 分；解析不正确得 0-4 分 |
| **总计** | **100** | — |

---

# HW4: 热图显示与融合体验 | Thermal Image Display & Fusion Experience

> **截止日期**：Day 13 结束时 | **权重**：28%（原 HW4 18% + 原 HW5 10% 已合并）| **预计耗时**：课后 4-5 小时
>
> **说明**：本作业由原 HW4（APP 渲染）+ HW5（图像融合）合并而来。主线从「在手机上开发 Android APP」改为「在 PC 上用 Python + OpenCV 显示热图、再叠加可见光做融合」，对齐体验档难度（不要求从零开发手机 App）。想挑战手机端的同学，colourfate 开源了现成 Android APP，可直接安装调参。

## 学习成果 | Learning Outcomes

完成本作业后，你将能够：

1. **识别** 一个 PC 端热图可视化程序的基本结构（串口接收 → 帧解析 → 伪彩映射 → 显示）以及 OpenCV 在图像处理中的作用
2. **解释** 伪彩色映射的原理以及不同配色方案（Ironbow、Rainbow、White Hot）的适用场景
3. **实现** 一个能通过 USB CDC 接收温度数据、渲染伪彩色热图、并能叠加可见光做融合的 PC 端 Python 脚本
4. **分析** 插值算法对低分辨率热图视觉效果的影响，以及融合对信息呈现的帮助

## 为什么学这个 | Why This Matters

可视化是用户与设备交互的**窗口**。传感器采集的温度数据、USB 传输的帧数据，最终都要在屏幕上变成人眼可理解的热图。伪彩色映射是将"看不见"的红外温度数据变成"看得见"的彩色图像的关键技术——就像夜视仪把红外光转成可见光一样。

**日常类比**：伪彩色映射就像天气预报的温度图——不同温度用不同颜色表示（蓝色冷、红色热），让你一眼就能看出哪里冷哪里热。没有伪彩色，你看到的只是一堆数字（32×24 = 768 个温度值），根本无法理解。插值就像给马赛克图片做"磨皮"——虽然不能增加真实信息，但看起来更舒服。融合就像把素描（可见光，细节多）和水彩（热图，色彩多）叠在一起——既看清是什么、又看清哪里热。

## 前置知识 | Prerequisites

- HW3 已完成：USB CDC 稳定传输温度数据，docs/protocol.md 已写好
- 安装了 Python 3 + pyserial + opencv-python + numpy（`pip install pyserial opencv-python numpy`）
- 了解 Python 基本语法（变量、函数、循环、列表）；OpenCV 不熟没关系，本作业给参考代码
- 已阅读 HW3 的 docs/protocol.md 接口文档

## 任务清单 | Task List

### 任务 1: PC 端串口接收与帧解析 (难度: ⭐⭐)

**步骤**：

1. `pip install pyserial opencv-python numpy` 装好依赖
2. 用 pyserial 打开 HW3 配好的 USB CDC 虚拟串口（Windows 上是 COMx，Linux 是 /dev/ttyACMx）
3. 按 HW3 的 protocol.md 帧格式（帧头 + 帧计数 + 1536 字节温度 + 校验）循环读取一帧
4. 校验通过后解析成 32×24 温度矩阵，打印中心点温度和帧计数确认链路通
5. 参考：`software/tests/test_basic.py` 里已有 `parse_thermal_packet` 等函数可直接复用

**预期产出**：scripts/usb_receiver.py — 能稳定接收并解析温度帧的 Python 脚本 + 控制台输出截图

**验证方法**：脚本运行后帧计数持续增长，中心温度在室温范围（20-30°C），手掌覆盖传感器时温度上升。

### 任务 2: 伪彩色映射与热图显示 (难度: ⭐⭐⭐)

**步骤**：

1. 写一个伪彩色映射函数：温度值 → 归一化 → 查颜色表 → RGB。先实现 Ironbow（蓝→紫→红→橙→黄→白）
2. 用 OpenCV 把 32×24 的彩色矩阵 resize 到 320×240（双线性插值，让画面更平滑）
3. 用 `cv2.imshow` 显示热图，叠加上中心温度、最高/最低温度文字
4. 再加一种配色方案（Rainbow），按键盘按键切换
5. 参考：`software/tests/test_basic.py` 已有伪彩映射 + 高斯模糊频率分解函数可复用

**预期产出**：热图显示代码 + 两种配色方案热图截图

**验证方法**：热图颜色分布合理，手掌测试有明显的热区，两种配色可切换。

### 任务 3: 可见光叠加与图像融合 (难度: ⭐⭐⭐)

**步骤**：

1. 用 `cv2.VideoCapture(0)` 打开电脑自带摄像头（或外接 USB 摄像头）取可见光画面，resize 到 320×240
2. 实现透明度叠加融合：`cv2.addWeighted(visible, 0.6, thermal, 0.4, 0, fused)`
3. 实现高频增强融合：把热图做高斯模糊得到低频，高频=热图-低频，再叠加到可见光上（`test_basic.py` 的高斯频率分解可参考）
4. 加键盘切换显示模式：纯热图 / 纯可见光 / 透明度叠加 / 高频增强
5. 注意：电脑摄像头和 MLX90640 物理位置不同，画面有视差，本项目接受近似对齐即可（不必做精确配准）

**预期产出**：四种融合模式的截图 + 融合算法代码

**验证方法**：融合效果可辨识，热区在可见光图上有明显颜色标注。

### 任务 4: 端到端联调与场景测试 (难度: ⭐⭐)

**步骤**：

1. 完成全链路数据流：MLX90640 → I2C → STM32 → USB → PC 脚本 → 显示
2. 测试三个场景并记录结果：室温场景、人体检测（人站到镜头前）、热水杯检测
3. 记录系统性能指标：端到端帧率、USB 通信稳定性、显示帧率
4. 记录发现的问题和解决方法（按现象→原因→解决格式）

**预期产出**：联调测试记录表 + 性能指标记录 + 问题与解决记录

**验证方法**：三个场景都能正常工作，端到端帧率 ≥ 2 FPS。

### 任务 5: 稳健性与错误处理 (难度: ⭐)

**步骤**：

1. USB 断开时脚本不崩溃，提示"设备已断开"并等待重连
2. 校验失败的帧跳过，不显示错帧
3. 摄像头未连接时退化为仅显示热图模式
4. 按 `s` 键截图保存当前画面到本地

**预期产出**：优化后的脚本代码 + 各功能截图

**验证方法**：拔插 USB 后脚本不崩溃、能恢复，截图功能可用。

## 理解验证问题 | Comprehension Check

**Q1（设计权衡题）**：Ironbow 和 Rainbow 配色方案哪个更好？

请从人眼感知、应用场景、色觉障碍三个角度分析。

> **参考答案**：
> 
> 1. **人眼感知**：Rainbow 色阶更丰富，但人眼对绿色亮度感知最敏感、对蓝色最不敏感，导致冷区分辨率低。Ironbow 亮度单调递增，更符合人眼感知特性。
> 2. **应用场景**：建筑检测推荐 Ironbow（亮度单调性让人一眼看出热点）；人体测温 Rainbow 可能更好（体温范围窄，Rainbow 色阶更丰富）。
> 3. **色觉障碍**：Ironbow 更友好——温度变化主要对应亮度变化，色盲用户可通过亮度区分。Rainbow 中红→绿变化对红绿色盲基本不可区分。这也是专业热像仪默认使用 Ironbow 的原因之一。

**Q2（原理追问题）**：插值放大能增加真实信息吗？

1. 插值放大的本质是什么？增加了什么？没有增加什么？
2. 如果两个相邻像素温度分别为 25°C 和 35°C，中间的插值值一定是 30°C 吗？

> **参考答案**：
> 
> 1. 插值放大的本质是"根据已知点估计未知点"。它增加了显示像素数（图像看起来更大更平滑），但没有增加信息量（真正的温度测量点仍然是 768 个）。插值假设"相邻像素之间温度平滑变化"，在温度突变区域（如边缘）可能不成立。
> 2. 不一定。双线性插值假设温度线性变化所以是 30°C，但实际物体温度分布可能不是线性的。更高级的插值方法在平滑区域效果更好，但在边缘处仍然无法恢复真实信息。

**Q3（类比迁移题）**：为什么双目热像仪需要两个摄像头而不是一个？

> **参考答案**：
> 
> 红外摄像头就像只能看到"温度"的医生——能告诉你哪里发热，但看不清那是什么。可见光摄像头像普通相机——能看清外观但不知道温度。双目热像仪就像一个既有"温度视觉"又有"形状视觉"的超级医生。只用红外的问题是 32×24 根本看不清物体形状——只能看到一团热一团冷。叠加可见光后可同时看到外观和温度分布，这才是真正有用的信息。专业热像仪（如 FLIR）都是双目的。

## 提交要求 | Submission Requirements

1. **代码**：scripts/usb_receiver.py — PC 端热图可视化 + 融合脚本（串口接收 + 伪彩映射 + 融合 + 显示）
2. **截图/照片**：hw4_ironbow.jpg + hw4_rainbow.jpg + hw4_fusion_alpha.jpg + hw4_fusion_hf.jpg + hw4_real_scene.jpg
3. **测试记录**：hw4_integration_test.md — 联调测试记录表（三个场景）
4. **理解验证回答**：hw4_comprehension.md（3 个问题，每个 150-250 字）

**代码规范检查清单**：
- [ ] 串口接收在循环里，有校验失败跳帧、USB 断开不崩溃的处理
- [ ] 伪彩色映射函数有注释说明温度→颜色的映射逻辑
- [ ] 至少实现两种配色方案（Ironbow + Rainbow）
- [ ] 至少实现两种融合模式（透明度叠加 + 高频增强），可切换
- [ ] 代码可直接复用 software/tests/test_basic.py 里的解析/映射函数

## 评分标准 | Rubric

| 维度 | 满分 | 评分细则 |
|------|------|---------|
| **任务完成度** | 35 | 任务 1（8分）+ 任务 2（8分）+ 任务 3（9分）+ 任务 4（6分）+ 任务 5（4分） |
| **理解验证质量** | 30 | 3 题每题 10 分。能从多角度深入分析（配色 / 插值 / 双目）得 24-30 分；有分析但浅显得 15-23 分；仅复述得 0-14 分 |
| **可视化与融合效果** | 20 | 两种配色 + 两种融合可切换、效果清晰得 16-20 分；基本显示热图得 10-15 分；仅搭框架得 0-9 分 |
| **代码质量** | 15 | 复用 test_basic.py 函数、注释完整、错误处理到位得 12-15 分；基本可用得 7-11 分；有明显问题得 0-6 分 |
| **总计** | **100** | — |

---

# HW5:（已并入 HW4） | Merged into HW4

> **说明**：本作业已并入 **HW4「热图显示与融合体验」**。
> 原本 HW4（PC 端热图显示）和 HW5（系统联调 + 图像融合）被拆成两份，但都围绕"把温度数据变成屏幕上的彩色热图、再叠加可见光做融合"这一件事，且共用同一个 PC 脚本（scripts/usb_receiver.py）。为避免学生在同一套脚本上交两次作业、并裁到体验档作业量，两份合并为 HW4 一份（权重 28%），含：串口接收、伪彩映射、双配色显示、双模式融合、端到端联调三场景、稳健性处理。
>
> 如果你已经按 HW4 完成了热图显示 + 融合 + 联调，本作业即视为完成，无需再单独提交。
>
> **想挑战手机端的同学**：colourfate 开源了完整的 Android APP（https://github.com/colourfate/ThermalEyes），可直接安装到手机连设备调参，作为进阶体验，不作为主线要求。

---

# HW6: 最终展示与项目文档 | Final Presentation & Project Documentation

> **截止日期**：Day 15 结束时 | **权重**：计入"演示展示"维度（25%，含原 Phase Review 5%）| **预计耗时**：课后 3-4 小时

## 学习成果 | Learning Outcomes

完成本作业后，你将能够：

1. **组织** 一场 5-8 分钟的技术演示，清晰展示项目成果
2. **撰写** 完整的项目文档（README、代码注释、协议文档），使项目可复现
3. **反思** 15 天学习过程中的关键收获和设计决策
4. **应对** 技术问答中"为什么这样设计"类问题

## 为什么学这个 | Why This Matters

项目做得再好，如果没法清楚地向别人展示和解释，它的价值就大打折扣。技术演示和文档是**从"做出来"到"交付"**的关键步骤——在大学和职场中，你经常需要向导师、同事、客户展示你的工作。能清楚解释"为什么这样做"比"做得有多完美"更能体现你的技术水平。

**日常类比**：写项目文档就像写菜谱——你做出的菜很好吃（项目能工作），但如果不能把做法写清楚（文档不完整），别人就没法复现。技术演示就像厨师上菜——不仅要端上桌，还要解释"为什么选这个食材、为什么用这个火候"。

## 前置知识 | Prerequisites

- HW5 已完成：系统端到端可工作
- 所有理解验证问题的回答已完成

## 任务清单 | Task List

### 任务 1: 演示 PPT 制作 (难度: ⭐⭐)

**步骤**：

1. 制作 10-15 页演示 PPT，包含：
   - **项目背景**（1-2 页）：热成像技术的应用场景、项目目标
   - **系统架构**（1-2 页）：硬件+固件+APP 整体框图、数据流说明
   - **关键技术**（3-4 页）：I2C 驱动、USB 通信、伪彩色映射、图像融合——每页讲一个技术点，重点解释"为什么这样设计"
   - **演示展示**（2-3 页）：实物照片、热图截图、融合效果截图
   - **问题与解决**（1-2 页）：主要困难和解决方法
   - **总结与展望**（1 页）：15 天收获、未来改进方向
2. PPT 中必须有系统架构图、数据流图
3. 关键技术页面必须有"设计权衡"内容

**预期产出**：docs/presentation.pptx

**验证方法**：PPT 包含所有要求章节，有图表和设计权衡分析。

### 任务 2: 项目文档完善 (难度: ⭐⭐)

**步骤**：

1. 完善项目 README.md：项目简介、系统架构图、硬件清单和接线说明、编译烧录步骤、APP 安装使用说明、通信协议链接、常见问题和调试指南
2. 确保代码有适当注释：模块文件开头有功能说明、关键函数有参数和返回值说明、复杂算法有步骤注释
3. 确保项目可复现

**预期产出**：完整的 README.md + 代码注释完善

**验证方法**：新同学按 README 操作能否成功运行。

### 任务 3: 实物演示 (难度: ⭐⭐⭐)

**步骤**：

1. 准备演示场景：室温扫描、人体热检测、热水杯检测
2. 进行至少一次完整预演，控制时间在 5-8 分钟
3. 准备备用方案（USB 连接问题时用视频/截图替代）
4. 正式演示中：清晰讲解架构和数据流、实物演示热图和融合效果、至少解释 2 个"为什么这样设计"的技术决策、回答评委问题

**预期产出**：演示完成 + 演示照片或视频

**验证方法**：演示流畅完成，能回答技术问题。

### 任务 4: 项目总结与反思（含原 Phase Review 的设计权衡附录） (难度: ⭐⭐)

**步骤**：

1. 写一份 800-1200 字的项目总结：关键收获、最困难时刻、重新做会改变什么、未来改进设想
2. 列出最重要的 3 个"设计权衡"决策及理由
3. **设计权衡附录（原 Phase Review 并入）**：完成下面的"系统架构设计权衡表"，每个维度至少 80 字说明为什么选当前方案而不是备选方案：

   | 维度 | 选择 | 备选方案 | 分析（你写） |
   |------|------|---------|------|
   | MCU | STM32F411 | ESP32、Arduino | 从性能、USB 支持、开发难度三个角度比较 |
   | 传感器通信 | I2C | SPI、UART | 从引脚数、速度、多设备支持三个角度比较 |
   | 数据传输 | USB CDC | BLE、WiFi | 从延迟、带宽、复杂度三个角度比较 |
   | 可视化方式 | PC 端 Python+OpenCV | 手机 Android APP、Web | 从开发难度、USB 接入、图像处理生态三个角度比较 |
   | 图像融合 | 高频增强 | 透明度叠加 | 从视觉效果、计算复杂度、信息保留三个角度比较 |

4. **数据流瓶颈分析（附录续）**：画出完整数据流图（MLX90640 → I2C → STM32 → USB → PC 脚本 → 显示），标注每环节的数据量（字节）和耗时（毫秒），指出系统最大瓶颈并提出一个优化方案

**预期产出**：docs/reflection.md（含总结 + 设计权衡表 + 数据流瓶颈分析）

**验证方法**：总结真诚有深度不是流水账；设计权衡表每个维度有实质分析（不是只填选择）；瓶颈定位合理。

## 理解验证问题 | Comprehension Check

**Q1（设计权衡题）**：如果重新设计，你会选择什么不同的技术方案？

从 MCU 选择、通信方式、可视化方式三个角度分析。

> **参考答案**：（开放题）
> 
> 1. STM32F411 性能和 USB 支持合理。降低难度可考虑 ESP32-S3（WiFi/BLE、双核、USB 支持），但 I2C 性能不如 STM32 且 HAL 库不如其成熟。
> 2. USB CDC 最直接。BLE 5.0 带宽勉强够用但延迟和复杂度增加。WiFi 带宽更高但功耗和复杂度也更高。
> 3. 本项目主线用 PC 端 Python+OpenCV 做可视化，开发门槛低、图像处理生态成熟。想做成手机端产品，Android APP（colourfate 开源版可参考）更便携但要面对 USB Host/JNI 等额外复杂度；Web 方案（WebUSB）理论可行但兼容性差。教学场景下 PC 端最省事，产品场景下 Android 最实用。

**Q2（类比迁移题）**：如何向一个不懂技术的人解释你的项目？

请用日常生活中的类比解释"双目热成像仪"。

> **参考答案**：
> 
> 想象你的眼睛能看到两种不同的东西：普通眼睛看到日常世界（可见光），还有一种"温度眼睛"能看到东西的热度（红外光）。我们的项目就是给电脑（或手机）装上这双"温度眼睛"——一个小设备通过 USB 连到电脑，电脑屏幕同时显示普通画面和温度画面。
> 
> 设备里面的小芯片像翻译官——把传感器"看到"的温度数据翻译成电脑能理解的语言，通过 USB 线传给电脑。电脑上的小程序像画师，把温度数据画成彩色温度图：红色热、蓝色冷。最后把温度图和普通照片叠在一起，一眼就能看出"哪里热、哪里冷"。

## 提交要求 | Submission Requirements

1. **文档**：README.md + docs/presentation.pptx + docs/reflection.md（含原 Phase Review 的设计权衡附录）+ docs/protocol.md
2. **代码**：最终版固件和 PC 可视化脚本（scripts/usb_receiver.py），全部提交到 Git 仓库
3. **演示**：完成现场演示
4. **理解验证回答**：hw6_comprehension.md（2 个问题，每个 150-300 字）

## 评分标准 | Rubric

| 维度 | 满分 | 评分细则 |
|------|------|---------|
| **PPT 质量** | 25 | 结构完整、视觉清晰、有架构图和设计权衡分析得 20-25 分；基本完整得 12-19 分；简陋得 0-11 分 |
| **文档完整性** | 25 | README 完整可复现、代码注释充分、协议文档详细得 20-25 分；基本完整得 12-19 分；不足得 0-11 分 |
| **演示表现** | 25 | 流畅清晰、功能完整、能回答"为什么"类问题得 20-25 分；基本完成得 12-19 分；演示失败得 0-11 分 |
| **反思深度（含原 Phase Review）** | 25 | 反思真诚有深度、设计权衡表每维度有实质分析、数据流瓶颈定位合理得 20-25 分；有反思但权衡表单薄得 12-19 分；流水账得 0-11 分 |
| **总计** | **100** | — |

---

# Check-in 1: Phase 1-2 进度汇报 | Phase 1-2 Progress Report

> **截止日期**：Day 7 结束时 | **权重**：8%

## 汇报要求

- 字数：600-1200 字（中文）
- 必须包含：

### 1. 已完成的功能列表

列出 Phase 1（Day 1-3）和 Phase 2（Day 4-7）中完成的所有功能，配截图/照片：
- 环境搭建完成情况
- LED 闪烁和按键控制
- I2C 总线扫描结果
- 传感器驱动开发进度
- 温度数据读取和计算

### 2. 遇到的主要困难和解决方案

至少列出 3 个技术问题，按以下格式：
- **现象**：你看到了什么（报错信息、异常行为）
- **原因分析**：你认为根本原因是什么
- **解决方法**：你是怎么解决的

### 3. 下阶段计划

逐日列出 Day 8-10 的预期目标（USB CDC 配置、数据传输、固件集成）。

### 4. 个人反思

- 到目前为止，你学到的最重要的一个技术概念是什么？
- 哪里可以做得更好？

## 评分标准 | Rubric

| 维度 | 满分 | 评分细则 |
|------|------|---------|
| **内容详细度** | 35 | 功能列表完整、有截图佐证得 28-35 分；基本完整但缺少截图得 18-27 分；内容简略得 0-17 分 |
| **问题分析深度** | 30 | 每个问题有完整的现象→原因→解决链路得 24-30 分；有分析但不深入得 15-23 分；仅罗列问题无分析得 0-14 分 |
| **计划的可行性** | 20 | 计划具体可行、与课程进度对齐得 16-20 分；计划过于模糊得 10-15 分；无计划得 0-9 分 |
| **反思的真诚度** | 15 | 反思真诚有深度、有具体例子得 12-15 分；有反思但泛泛而谈得 7-11 分；敷衍了事得 0-6 分 |
| **总计** | **100** | — |

---

# Check-in 2: Phase 3-4 进度汇报 | Phase 3-4 Progress Report

> **截止日期**：Day 13 结束时 | **权重**：8%

## 汇报要求

- 字数：600-1200 字（中文）
- 必须包含：

### 1. 已完成的功能列表

列出 Phase 3（Day 8-10）和 Phase 4（Day 11-13）中完成的所有功能，配截图/照片：
- USB CDC 通信实现
- 数据帧协议设计与验证
- 固件集成测试
- PC 端可视化脚本开发进度
- 伪彩色热图渲染效果

### 2. 遇到的主要困难和解决方案

至少列出 3 个技术问题，按格式：现象→原因分析→解决方法

### 3. 下阶段计划

逐日列出 Day 14-15 的预期目标（系统联调、演示准备）。

### 4. 设计决策回顾

回顾 Day 1-13 的开发过程，列出最重要的 2 个"设计权衡"决策，解释：
- 你当时面临的选择
- 你选择的方案
- 现在回头看，这个决策是否正确？如果重新做，你会改变吗？

## 评分标准 | Rubric

| 维度 | 满分 | 评分细则 |
|------|------|---------|
| **内容详细度** | 30 | 功能列表完整、有截图佐证得 24-30 分；基本完整但缺少截图得 15-23 分；内容简略得 0-14 分 |
| **问题分析深度** | 25 | 每个问题有完整的现象→原因→解决链路得 20-25 分；有分析但不深入得 12-19 分；仅罗列问题无分析得 0-11 分 |
| **设计决策回顾** | 25 | 能清晰描述设计权衡、有理有据、有回顾反思得 20-25 分；有描述但缺乏反思得 12-19 分；未涉及设计权衡得 0-11 分 |
| **计划的可行性** | 10 | 计划具体可行得 8-10 分；过于模糊得 5-7 分；无计划得 0-4 分 |
| **反思的真诚度** | 10 | 反思真诚有深度得 8-10 分；泛泛而谈得 5-7 分；敷衍了事得 0-4 分 |
| **总计** | **100** | — |

---

# Phase Review:（已并入 HW6） | Merged into HW6

> **说明**：原 Phase Review（技术复盘与设计权衡分析，独立 5% 权重）已并入 **HW6 任务 4「项目总结与反思」**，作为其中的"设计权衡附录 + 数据流瓶颈分析"部分。
> 这样做是为了裁到体验档作业量：Phase Review 和 HW6 反思考察的是同一种能力——把整个项目的技术选择想清楚、说清楚——拆成两份独立作业会让学生在相近内容上写两遍。
> 原要求（系统架构设计权衡表、数据流瓶颈分析、逆向分析）的完整内容已移入 HW6 任务 4 的附录，详见 HW6。
>
> 权重处理：原 Phase Review 的 5% 转入"演示展示（HW6）"维度，该维度从 20% 调整为 25%。

---

## 总成绩计算 | Overall Grade Calculation

`
总成绩 = HW1 × 15% + Check-in1 × 8% + HW2 × 18% + HW3 × 18% + Check-in2 × 8%
       + HW4 × 28%（原 HW4 18% + 原 HW5 10% 已合并）

注：各 HW 和 Check-in 按百分制评分后乘以权重。
    原 Phase Review（5%）已并入 HW6 反思，计入"演示展示"维度（总评的 25%）。
`

### 权重验证

| 维度 | 组成 | 总权重 |
|------|------|--------|
| 作业链 | HW1(15%) + HW2(18%) + HW3(18%) + HW4(28%，含原 HW5) | 79% |
| 进度汇报 | Check-in1(8%) + Check-in2(8%) | 16% |
| 技术复盘 | （已并入 HW6 反思，计入演示展示维度） | 0% |
| **合计** | | **95%**（另 5% 由原 Phase Review 转入演示展示维度） |

> *注：演示展示（HW6）作为独立维度占总评 25%（含原 Phase Review 5%），与总评分中的"技术实现(40%)+文档质量(20%)+进度汇报(10%)+团队协作(10%)"并列计算。*

### 独立学员说明

独立完成项目的学员，"团队协作"维度（10%）转换为"自主学习"维度，考察独立解决问题和自主查阅资料的能力。该维度作为附加加减分项（±5 分），不单独占比。

---

*最后更新：2026-06-23*
*Last Updated: 2026-06-23*
